apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-200214-w95a-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-200214-w95a.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-200214-w95a-service
            port:
              number: 80
